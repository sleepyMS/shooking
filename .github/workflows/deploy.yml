name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install
        working-directory: ./shooking

      - name: Build React app
        run: npm run build
        working-directory: ./shooking

      - name: Build Storybook
        run: npm run build-storybook
        working-directory: ./shooking

      # 첫 번째 배포: 스토리북을 루트에 배포
      - name: Deploy to GitHub Pages (Storybook)
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./shooking/storybook-static
          destination_dir: ./

      # 두 번째 배포: 프론트엔드 페이지를 루트에 배포 (이것이 덮어쓰게 됨)
      - name: Deploy to GitHub Pages (Frontend)
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./shooking/build
          destination_dir: ./
